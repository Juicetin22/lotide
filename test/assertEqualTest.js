//was just testing!
/*const assert = require('chai').assert;
const assertEqual = require('../assertEqual');

describe('two identical primitive data values should equal', () => {

  it('return false when two values are different', () => {
    assert.strictEqual(assertEqual("Lighthouse", "Bootcamp"), false);
  });

  it('should pass when given two equal values', () => {
    assert.strictEqual(assertEqual(1, 1), true);
  });

});*/